import 'package:flutter/material.dart';

class ClientHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('صفحة العميل')),
      body: Center(child: Text('أهلاً بك عميل ASFA')),
    );
  }
}
