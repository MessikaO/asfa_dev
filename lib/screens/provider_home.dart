import 'package:flutter/material.dart';

class ProviderHome extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('صفحة المزود')),
      body: Center(child: Text('أهلاً بك مزود ASFA')),
    );
  }
}
